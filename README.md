Project demonstrates an Android Studio bug where the app cannot be started in the debugger
when it specifies a secondary process such as ":appext" in the <application> Manifest tag or
the <activity> tag.
